SECRET_KEY=replace_with_a_long_random_secret
SESSION_COOKIE_SECURE=false

# Database
# Local default is SQLite if DATABASE_URL is empty.
# For Render PostgreSQL:
# DATABASE_URL=postgresql+psycopg://user:password@host:5432/dbname
DATABASE_URL=

# Email reset delivery
# MAIL_MODE=console (dev) OR smtp (production)
MAIL_MODE=console
MAIL_FROM=noreply@secureshield.app
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=true

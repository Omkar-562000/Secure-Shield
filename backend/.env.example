# ------------------------------------------------------------------
# 1) App security
# ------------------------------------------------------------------
# Generate locally:
# python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=replace_with_a_long_random_secret

# local dev (http): false
# production (https): true
SESSION_COOKIE_SECURE=false


# ------------------------------------------------------------------
# 2) Database (CURRENT backend uses SQLAlchemy + DATABASE_URL)
# ------------------------------------------------------------------
# Local default is SQLite if DATABASE_URL is empty.
DATABASE_URL=

# If using Postgres (Render/Neon/Supabase/Railway), get connection URL
# from your DB dashboard and use SQLAlchemy driver format:
# DATABASE_URL=postgresql+psycopg://user:password@host:5432/dbname


# ------------------------------------------------------------------
# 3) MongoDB (FOR FUTURE MIGRATION ONLY)
# ------------------------------------------------------------------
# Current app.py does not use MONGODB_URI yet.
# Use this only after migrating backend queries/models to MongoDB.
# Get this from MongoDB Atlas:
# Atlas -> Database -> Connect -> Drivers -> Python
MONGODB_URI=


# ------------------------------------------------------------------
# 4) Email reset delivery
# ------------------------------------------------------------------
# dev: MAIL_MODE=console (code printed in backend logs)
# prod: MAIL_MODE=smtp (real email delivery)
MAIL_MODE=console
MAIL_FROM=noreply@secureshield.app

# Get SMTP values from your email provider dashboard
# (Brevo / SendGrid / Gmail App Password / Mailgun)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=true
